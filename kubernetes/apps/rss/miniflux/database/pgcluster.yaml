---
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: pg-miniflux
spec:
  instances: 1
  managed:
    services:
      disabledDefaultServices: ["ro", "r"]
  postgresql:
    parameters:
      timezone: ${CLUSTER_TZ}
  storage:
    size: 1G
    storageClass: democratic-csi-hostpath
  monitoring:
    enablePodMonitor: true

  backup:
    retentionPolicy: 30d
    barmanObjectStore:
      wal:
        compression: zstd
      serverName: pg-miniflux-v4
      destinationPath: s3://cnpg-backups/
      endpointURL: "https://s3.${CLUSTER_DOMAIN}"
      s3Credentials:
        accessKeyId:
          name: cloudnative-pg-secret
          key: AWS_ACCESS_KEY_ID
        secretAccessKey:
          name: cloudnative-pg-secret
          key: AWS_SECRET_ACCESS_KEY

  bootstrap:
    # initdb:
    #   database: miniflux
    #   owner: miniflux
    recovery:
      source: source
      database: miniflux
      owner: miniflux

  externalClusters:
    - name: source
      barmanObjectStore:
        wal:
          compression: zstd
        serverName: pg-miniflux-v3
        destinationPath: s3://cnpg-backups/
        endpointURL: "https://s3.${CLUSTER_DOMAIN}"
        s3Credentials:
          accessKeyId:
            name: cloudnative-pg-secret
            key: AWS_ACCESS_KEY_ID
          secretAccessKey:
            name: cloudnative-pg-secret
            key: AWS_SECRET_ACCESS_KEY
