---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: renovate
spec:
  refreshInterval: 15m
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault
  target:
    name: renovate-secret
    template:
      data:
        RENOVATE_DOCKER_DOCKER_IO_USERNAME: "{{ .DOCKER_IO_USERNAME }}"
        RENOVATE_DOCKER_DOCKER_IO_PASSWORD: "{{ .DOCKER_IO_PASSWORD }}"
        RENOVATE_GITHUB_COM_TOKEN: "{{ .OPENCLAW_TOKEN }}"
        RENOVATE_TOKEN: "{{ .RENOVATE_TOKEN }}"
        RENOVATE_WEBHOOK_TOKEN: "{{ .RENOVATE_WEBHOOK_TOKEN }}"
  dataFrom:
    - extract:
        key: docker
    - extract:
        key: github
    - extract:
        key: renovate
