---
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: pg-danbooru
spec:
  instances: 1
  managed:
    services:
      disabledDefaultServices: ["ro", "r"]
  postgresql:
    parameters:
      timezone: ${CLUSTER_TZ}
  storage:
    size: 10G
    storageClass: democratic-csi-hostpath
  monitoring:
    enablePodMonitor: true

  backup:
    retentionPolicy: 30d
    barmanObjectStore:
      wal:
        compression: zstd
      serverName: pg-danbooru-v2
      destinationPath: s3://cnpg-backups/
      endpointURL: "https://s3.${CLUSTER_DOMAIN}"
      s3Credentials:
        accessKeyId:
          name: danbooru-pg-secret
          key: AWS_ACCESS_KEY_ID
        secretAccessKey:
          name: danbooru-pg-secret
          key: AWS_SECRET_ACCESS_KEY

  bootstrap:
    # initdb:
    #   database: danbooru
    #   owner: danbooru
    recovery:
      source: source
      database: danbooru
      owner: danbooru

  externalClusters:
    - name: source
      barmanObjectStore:
        wal:
          compression: zstd
        serverName: pg-danbooru-v1
        destinationPath: s3://cnpg-backups/
        endpointURL: "https://s3.${CLUSTER_DOMAIN}"
        s3Credentials:
          accessKeyId:
            name: danbooru-pg-secret
            key: AWS_ACCESS_KEY_ID
          secretAccessKey:
            name: danbooru-pg-secret
            key: AWS_SECRET_ACCESS_KEY
