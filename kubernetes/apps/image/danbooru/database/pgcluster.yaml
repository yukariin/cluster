---
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: pg-danbooru
  annotations:
    cnpg.io/skipEmptyWalArchiveCheck: "enabled"
spec:
  instances: 1
  # Single-instance cluster: disable PDB so the node hosting it can be drained
  # for Talos/Kubernetes upgrades (CNPG blocks single-instance drains otherwise).
  enablePDB: false
  managed:
    services:
      disabledDefaultServices:
        - "ro"
        - "r"
  storage:
    size: 10G
    storageClass: ceph-block
  monitoring:
    enablePodMonitor: true

  bootstrap:
    # initdb:
    #   database: danbooru
    #   owner: danbooru
    recovery:
      source: pg-danbooru-restore
      database: danbooru
      owner: danbooru

  plugins:
    - name: barman-cloud.cloudnative-pg.io
      isWALArchiver: true
      parameters:
        barmanObjectName: pg-danbooru-store
        serverName: pg-danbooru

  externalClusters:
    - name: pg-danbooru-restore
      plugin:
        name: barman-cloud.cloudnative-pg.io
        parameters:
          barmanObjectName: pg-danbooru-store
          serverName: pg-danbooru
