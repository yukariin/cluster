---
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: pg-netbird
spec:
  instances: 1
  managed:
    services:
      disabledDefaultServices: ["ro", "r"]
  postgresql:
    parameters:
      timezone: ${CLUSTER_TZ}
  storage:
    size: 5G
    storageClass: democratic-csi-hostpath

  backup:
    retentionPolicy: 30d
    barmanObjectStore:
      wal:
        compression: bzip2
      serverName: pg-netbird-v2
      destinationPath: s3://cnpg-backups/
      endpointURL: "http://192.168.1.10:9000"
      s3Credentials:
        accessKeyId:
          name: cloudnative-pg-secret
          key: AWS_ACCESS_KEY_ID
        secretAccessKey:
          name: cloudnative-pg-secret
          key: AWS_SECRET_ACCESS_KEY

  bootstrap:
    # initdb:
    #   database: netbird
    #   owner: netbird
    recovery:
      source: source
      database: netbird
      owner: netbird

  externalClusters:
    - name: source
      barmanObjectStore:
        wal:
          compression: bzip2
        serverName: pg-netbird
        destinationPath: s3://cnpg-backups/
        endpointURL: "http://192.168.1.10:9000"
        s3Credentials:
          accessKeyId:
            name: cloudnative-pg-secret
            key: AWS_ACCESS_KEY_ID
          secretAccessKey:
            name: cloudnative-pg-secret
            key: AWS_SECRET_ACCESS_KEY
